spring:
  jackson:
      serialization.indent_output: true
  datasource:
    url: jdbc:mysql://${DATABASE_ADDRESS:192.168.99.100}/${DATABASE_NAME:faf_test}?useSSL=false
    name: faf_lobby
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:banana}
  jpa:
    show-sql: true

logging:
  level:
    org.springframework: warn
    com.faforever.server: debug

